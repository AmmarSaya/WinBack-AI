{
  "name": "@winback/drainer-app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "vitest run -c vitest.integration.config.ts"
  },
  "dependencies": {
    "@prisma/client": "5.22.0",
    "@winback/ai": "workspace:*",
    "@winback/config": "workspace:*",
    "@winback/contracts": "workspace:*",
    "@winback/crypto": "workspace:*",
    "@winback/db": "workspace:*",
    "@winback/email": "workspace:*",
    "@winback/errors": "workspace:*",
    "@winback/logger": "workspace:*",
    "@winback/queue": "workspace:*",
    "@winback/shopify": "workspace:*",
    "bullmq": "5.79.2",
    "ioredis": "5.10.1",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "tsx": "^4.22.4",
    "vitest": "2.1.9"
  }
}
