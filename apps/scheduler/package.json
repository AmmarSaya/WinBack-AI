{
  "name": "@winback/scheduler-app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@prisma/client": "7.8.0",
    "@winback/config": "workspace:*",
    "@winback/contracts": "workspace:*",
    "@winback/crypto": "workspace:*",
    "@winback/db": "workspace:*",
    "@winback/errors": "workspace:*",
    "@winback/logger": "workspace:*",
    "@winback/queue": "workspace:*",
    "@winback/shopify": "workspace:*",
    "bullmq": "5.77.6",
    "ioredis": "5.10.1"
  },
  "devDependencies": {
    "tsx": "^4.22.4",
    "vitest": "2.1.9"
  }
}
